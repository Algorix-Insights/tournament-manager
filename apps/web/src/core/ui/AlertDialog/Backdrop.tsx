import { AlertDialogBackdrop as HeroAlertDialogBackdrop } from '@heroui/react'
import type { ComponentProps } from 'react'

export default function AlertDialogBackdrop(props: Readonly<ComponentProps<typeof HeroAlertDialogBackdrop>>) {
  return <HeroAlertDialogBackdrop {...props} />
}

