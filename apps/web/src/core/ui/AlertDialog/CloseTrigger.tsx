import { AlertDialogCloseTrigger as HeroAlertDialogCloseTrigger } from '@heroui/react'
import type { ComponentProps } from 'react'

export default function AlertDialogCloseTrigger(props: Readonly<ComponentProps<typeof HeroAlertDialogCloseTrigger>>) {
  return <HeroAlertDialogCloseTrigger {...props} />
}

