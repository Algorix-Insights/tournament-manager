import { AlertDialogFooter as HeroAlertDialogFooter } from '@heroui/react'
import type { ComponentProps } from 'react'

export default function AlertDialogFooter(props: Readonly<ComponentProps<typeof HeroAlertDialogFooter>>) {
  return <HeroAlertDialogFooter {...props} />
}

