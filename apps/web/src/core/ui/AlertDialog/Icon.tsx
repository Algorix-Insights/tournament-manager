import { AlertDialogIcon as HeroAlertDialogIcon } from '@heroui/react'
import type { ComponentProps } from 'react'

export default function AlertDialogIcon(props: Readonly<ComponentProps<typeof HeroAlertDialogIcon>>) {
  return <HeroAlertDialogIcon {...props} />
}

