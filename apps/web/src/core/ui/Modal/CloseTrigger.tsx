import { ModalCloseTrigger as HeroModalCloseTrigger } from '@heroui/react'
import type { ComponentProps } from 'react'

export default function ModalCloseTrigger(props: Readonly<ComponentProps<typeof HeroModalCloseTrigger>>) {
  return <HeroModalCloseTrigger {...props} />
}

