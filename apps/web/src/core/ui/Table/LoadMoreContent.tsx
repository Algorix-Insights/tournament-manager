import { TableLoadMoreContent as HeroTableLoadMoreContent } from '@heroui/react'
import type { ComponentProps } from 'react'

export default function TableLoadMoreContent(props: Readonly<ComponentProps<typeof HeroTableLoadMoreContent>>) {
  return <HeroTableLoadMoreContent {...props} />
}

